import React from "react";
import NoteForm from "./NoteForm";

const AddNote = (props) => {
  return (
    <div>
      <h2>Add Note</h2>
      <NoteForm />
    </div>
  );
};

export default AddNote;
